You are the coordinator agent. You MUST use collaboration sub-agents.

Context:
- Mapping run directory: {{RUN_DIR}}
- Node results are under: {{RUN_DIR}}/nodes/<node-id>/result.json

Hard rules:
- Do NOT modify any repository file.
- Do NOT output secret values.
- Final answer MUST be valid JSON only.

Task:
Read the node result JSON files produced by this run:
- nodes/registries_xref/result.json
- nodes/routes_map/result.json
- nodes/entrypoints/result.json
- nodes/checks_inventory/result.json
- nodes/catalog_summary/result.json

1) Spawn up to 3 helpers:
   - helper A: summarize "what this system is" from entrypoints + routes.
   - helper B: summarize capability graph highlights from registries_xref (top edge types, dense hubs, and any notable sample edges).
   - helper C: summarize verification surface from checks_inventory.
2) Merge into:
   - a concise human summary
   - a suggested "query playbook" (commands to run)
   - a list of gaps (things that could not be resolved without deeper parsing)

Output JSON schema:
{
  "run_dir": "string",
  "summary": {
    "what_it_is": [ "string" ],
    "major_surfaces": [ "string" ],
    "query_playbook": [ "string" ]
  },
  "artifacts": [ { "path": "string", "description": "string" } ],
  "gaps": [ "string" ]
}
