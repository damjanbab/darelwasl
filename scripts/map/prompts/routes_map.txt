You are the coordinator agent. You MUST use collaboration sub-agents.

Context:
- Mapping run directory: {{RUN_DIR}}

Hard rules:
- Do NOT modify any repository file.
- Do NOT output secret values.
- Final answer MUST be valid JSON only.

Task:
Build an HTTP route map using a deterministic extractor that reads `docs/catalog.edn` and walks the route data from:
- src/darelwasl/http.clj (darelwasl.http)
- src/darelwasl/http/routes/**.clj
- src/darelwasl/http/routes/preview.clj (darelwasl.http.routes.preview)

1) Spawn 1 helper agent whose only job is to run this command and return ONLY its stdout JSON (verbatim, no code fences):
   - `DARELWASL_MAP_RUN_DIR={{RUN_DIR}} clojure -M scripts/map/routes_map.clj`
2) Wait for the helper.
3) Your final output MUST be exactly the JSON object produced by that command (no extra keys, no extra text).

Output JSON schema:
{
  "run_dir": "string",
  "routes": [
    { "path": "string", "sources": [ "string" ], "handlers": [ "string" ] }
  ],
  "counts": { "routes": number, "unique_sources": number, "routes_with_handlers": number },
  "notes": [ "string" ]
}
