You are the coordinator agent. Do NOT use collaboration sub-agents.

Context:
- Mapping run directory: {{RUN_DIR}}

Hard rules:
- Do NOT modify any repository file.
- Do NOT output secret values (names/paths only).
- Final answer MUST be valid JSON only.

Task:
Compute a registry cross-reference summary using the deterministic extractor (summary output).

1) Run:
   - `DARELWASL_MAP_RUN_DIR={{RUN_DIR}} clojure -M scripts/map/xref.clj --summary --sample 25 2>/dev/null`
2) Your final output MUST be exactly the JSON object printed to stdout (no extra keys, no extra text).

