## What changed?
- …

## Why?
- …

## Verification
- [ ] Ran `scripts/checks.sh all`
- [ ] If `registries/` changed: verified the relevant targeted check(s) (`registries` / `schema` / `actions` / `app-smoke`)
- [ ] If UI-visible: verified via `scripts/checks.sh app-smoke`

## Contract updates
- [ ] Updated `registries/*.edn` if the capability surface changed

