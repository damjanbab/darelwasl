[
 {:id :cap/action/auth-login
  :title "Login"
  :version "v1"
  :inputs {:user/username :string
           :user/password :string}
  :outputs {:session/token :string
            :user/id :uuid
            :user/roles [:set :keyword]}
  :side-effects ["Create session for user"]
  :adapter {:integration :cap/integration/local-session}
  :audit {:records [:auth/login]
          :fields [:user/username]}
  :idempotency {:strategy :dedupe-on
                :keys [:user/username]}
  :contracts {:fixtures [:fixtures/users]}
  :compatibility {:backward true
                  :forward true
                  :flags []}
 :related {:schema [:cap/schema/user]
            :views [:cap/view/login]}
  :tooling [:cap/tooling/action-contract]}

 {:id :cap/action/user-list
  :title "List Users"
  :version "v1"
  :inputs {}
  :outputs {:users :vector}
  :side-effects []
  :adapter {:integration :local/db}
  :audit {:records [:user/read]
          :fields []}
  :idempotency {:strategy :pure}
  :contracts {:fixtures [:fixtures/users]}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/user]
            :views [:cap/view/user-management]}
  :tooling []}

 {:id :cap/action/user-create
  :title "Create User"
  :version "v1"
  :inputs {:user/username :string
           :user/password :string
           :user/name :string?
           :user/roles [:set :keyword]}
  :outputs {:user/id :uuid}
  :side-effects ["Persist user"]
  :adapter {:integration :local/db}
  :audit {:records [:user/change]
          :fields [:user/id :user/username]}
  :idempotency {:strategy :dedupe-on
                :keys [:user/username]}
  :contracts {:fixtures [:fixtures/users]}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/user]
            :views [:cap/view/user-management]}
  :tooling []}

 {:id :cap/action/user-update
  :title "Update User"
  :version "v1"
  :inputs {:user/id :uuid
           :user/username :string?
           :user/password :string?
           :user/name :string?
           :user/roles [:set :keyword]}
  :outputs {:user/id :uuid}
  :side-effects ["Persist user updates"]
  :adapter {:integration :local/db}
  :audit {:records [:user/change]
          :fields [:user/id :user/username]}
  :idempotency {:strategy :dedupe-on
                :keys [:user/id]}
  :contracts {:fixtures [:fixtures/users]}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/user]
            :views [:cap/view/user-management]}
  :tooling []}

 {:id :cap/action/user-delete
  :title "Delete User"
  :version "v1"
  :inputs {:user/id :uuid}
  :outputs {:user/id :uuid}
  :side-effects ["Remove user"]
  :adapter {:integration :local/db}
  :audit {:records [:user/change]
          :fields [:user/id :user/username]}
  :idempotency {:strategy :dedupe-on
                :keys [:user/id]}
  :contracts {:fixtures [:fixtures/users]}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/user]
            :views [:cap/view/user-management]}
  :tooling []}

 {:id :cap/action/task-create
  :title "Create Task"
  :version "v1"
 :inputs {:task/title :string
           :task/description :string
           :task/status :keyword
           :task/assignee :uuid
           :task/client :uuid
           :task/due-date :instant?
           :task/priority :keyword
           :task/tags :uuid-set
           :note/body :string?}
  :outputs {:task/id :uuid}
  :side-effects ["Persist task" "Create pending-reason note when status = :pending"]
  :adapter {:integration :local/db}
  :audit {:records [:task/change]
          :fields [:task/id :task/status :task/assignee]}
  :idempotency {:strategy :dedupe-on
                :keys [:task/title :task/assignee]}
  :contracts {:fixtures [:fixtures/tasks]}
  :compatibility {:backward true
                  :forward true
                  :flags [:task/extended?]}
  :related {:schema [:cap/schema/task]
            :views [:cap/view/tasks]}
  :tooling [:cap/tooling/action-contract]}

 {:id :cap/action/task-update
  :title "Update Task"
  :version "v1"
 :inputs {:task/id :uuid
           :task/title :string?
           :task/description :string?
           :task/priority :keyword?
           :task/tags :uuid-set?
           :task/client :uuid?}
  :outputs {:task/id :uuid}
  :side-effects ["Persist changes"]
  :adapter {:integration :local/db}
  :audit {:records [:task/change]
          :fields [:task/id]}
  :idempotency {:strategy :dedupe-on
                :keys [:task/id]}
  :contracts {:fixtures [:fixtures/tasks]}
  :compatibility {:backward true
                  :forward true
                  :flags [:task/extended?]}
  :related {:schema [:cap/schema/task]
            :views [:cap/view/tasks]}
  :tooling [:cap/tooling/action-contract]}

{:id :cap/action/task-set-status
  :title "Set Task Status"
  :version "v1"
  :inputs {:task/id :uuid
           :task/status :keyword
           :note/body :string?}
  :outputs {:task/id :uuid
            :task/status :keyword}
  :side-effects ["Persist status change" "Create pending-reason note when status = :pending"]
  :adapter {:integration :local/db}
  :audit {:records [:task/change]
          :fields [:task/id :task/status]}
  :idempotency {:strategy :dedupe-on
                :keys [:task/id :task/status]}
  :contracts {:fixtures [:fixtures/tasks]}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/task]
            :views [:cap/view/tasks]}
  :tooling [:cap/tooling/action-contract]}

 {:id :cap/action/task-assign
  :title "Assign Task"
  :version "v1"
  :inputs {:task/id :uuid
           :task/assignee :uuid}
  :outputs {:task/id :uuid
            :task/assignee :uuid}
  :side-effects ["Persist assignment"]
  :adapter {:integration :local/db}
  :audit {:records [:task/change]
          :fields [:task/id :task/assignee]}
  :idempotency {:strategy :dedupe-on
                :keys [:task/id :task/assignee]}
  :contracts {:fixtures [:fixtures/tasks :fixtures/users]}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/task :cap/schema/user]
            :views [:cap/view/tasks]}
  :tooling [:cap/tooling/action-contract]}

{:id :cap/action/task-set-due
  :title "Set Due Date"
  :version "v1"
  :inputs {:task/id :uuid
           :task/due-date :instant?}
  :outputs {:task/id :uuid
            :task/due-date :instant?}
  :side-effects ["Persist due date"]
  :adapter {:integration :local/db}
  :audit {:records [:task/change]
          :fields [:task/id :task/due-date]}
  :idempotency {:strategy :dedupe-on
                :keys [:task/id :task/due-date]}
  :contracts {:fixtures [:fixtures/tasks]}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/task]
            :views [:cap/view/tasks]}
  :tooling [:cap/tooling/action-contract]}

 {:id :cap/action/task-set-client
  :title "Set Task Client"
  :version "v1"
  :inputs {:task/id :uuid
           :task/client :uuid}
  :outputs {:task/id :uuid
            :task/client :uuid}
  :side-effects ["Persist client link"]
  :adapter {:integration :local/db}
  :audit {:records [:task/change]
          :fields [:task/id :task/client]}
  :idempotency {:strategy :dedupe-on
                :keys [:task/id :task/client]}
  :contracts {:fixtures [:fixtures/tasks]}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/task :cap/schema/client]
            :views [:cap/view/tasks]}
  :tooling [:cap/tooling/action-contract]}

 {:id :cap/action/client-create
  :title "Create Client"
  :version "v1"
  :inputs {:client/name :string
           :client/phone :string?
           :client/email :string?
           :client/channel :keyword?
           :client/status :keyword?
           :client/notes :string?}
  :outputs {:client/id :uuid}
  :side-effects ["Persist client"]
  :adapter {:integration :local/db}
  :audit {:records [:client/change]
          :fields [:client/id :client/name]}
  :idempotency {:strategy :dedupe-on
                :keys [:client/name]}
  :contracts {:fixtures [:fixtures/clients]}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/client]
            :views [:cap/view/tasks]}
  :tooling []}

 {:id :cap/action/client-update
  :title "Update Client"
  :version "v1"
  :inputs {:client/id :uuid
           :client/name :string?
           :client/phone :string?
           :client/email :string?
           :client/channel :keyword?
           :client/status :keyword?
           :client/notes :string?}
  :outputs {:client/id :uuid}
  :side-effects ["Persist client updates"]
  :adapter {:integration :local/db}
  :audit {:records [:client/change]
          :fields [:client/id :client/name]}
  :idempotency {:strategy :dedupe-on
                :keys [:client/id]}
  :contracts {:fixtures [:fixtures/clients]}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/client]
            :views [:cap/view/tasks]}
  :tooling []}

 {:id :cap/action/task-set-tags
  :title "Set Task Tags"
  :version "v1"
 :inputs {:task/id :uuid
           :task/tags :uuid-set}
 :outputs {:task/id :uuid
            :task/tags :uuid-set}
  :side-effects ["Persist tags"]
  :adapter {:integration :local/db}
  :audit {:records [:task/change]
          :fields [:task/id :task/tags]}
  :idempotency {:strategy :dedupe-on
                :keys [:task/id :task/tags]}
  :contracts {:fixtures [:fixtures/tasks]}
  :compatibility {:backward true
                  :forward true
                  :flags [:task/extended?]}
  :related {:schema [:cap/schema/task]
            :views [:cap/view/tasks]}
  :tooling [:cap/tooling/action-contract]}

 {:id :cap/action/task-archive
  :title "Archive Task"
  :version "v1"
  :inputs {:task/id :uuid
           :task/archived? :boolean}
  :outputs {:task/id :uuid
            :task/archived? :boolean}
  :side-effects ["Persist archived flag"]
  :adapter {:integration :local/db}
  :audit {:records [:task/change]
          :fields [:task/id :task/archived?]}
  :idempotency {:strategy :dedupe-on
                :keys [:task/id :task/archived?]}
  :contracts {:fixtures [:fixtures/tasks]}
 :compatibility {:backward true
                  :forward true
                  :flags []}
 :related {:schema [:cap/schema/task]
           :views [:cap/view/tasks]}
 :tooling [:cap/tooling/action-contract]}

 {:id :cap/action/task-add-note
  :title "Add Task Note"
  :version "v1"
  :inputs {:task/id :uuid
           :note/body :string
           :note/type :keyword?}
  :outputs {:task/id :uuid
            :note/id :uuid}
  :side-effects ["Persist note for task"]
  :adapter {:integration :local/db}
  :audit {:records [:task/change]
          :fields [:task/id]}
  :idempotency {:strategy :dedupe-on
                :keys [:task/id :note/body]}
  :contracts {:fixtures [:fixtures/tasks :fixtures/users]}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/task :cap/schema/note]
            :views [:cap/view/tasks]}
  :tooling [:cap/tooling/action-contract]}

 {:id :cap/action/task-edit-note
  :title "Edit Task Note"
  :version "v1"
  :inputs {:task/id :uuid
           :note/body :string}
  :outputs {:task/id :uuid
            :note/id :uuid}
  :side-effects ["Update latest comment note for task"]
  :adapter {:integration :local/db}
  :audit {:records [:task/change]
          :fields [:task/id]}
  :idempotency {:strategy :dedupe-on
                :keys [:task/id :note/body]}
  :contracts {:fixtures [:fixtures/tasks :fixtures/users]}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/task :cap/schema/note]
            :views [:cap/view/tasks]}
  :tooling [:cap/tooling/action-contract]}

 {:id :cap/action/task-delete-note
  :title "Delete Task Note"
  :version "v1"
  :inputs {:task/id :uuid}
  :outputs {:task/id :uuid
            :note/id :uuid}
  :side-effects ["Delete latest comment note for task"]
  :adapter {:integration :local/db}
  :audit {:records [:task/change]
          :fields [:task/id]}
  :idempotency {:strategy :dedupe-on
                :keys [:task/id]}
  :contracts {:fixtures [:fixtures/tasks :fixtures/users]}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/task :cap/schema/note]
            :views [:cap/view/tasks]}
  :tooling [:cap/tooling/action-contract]}

 {:id :cap/action/tag-create
  :title "Create Tag"
  :version "v1"
  :inputs {:tag/name :string}
  :outputs {:tag/id :uuid}
  :side-effects ["Persist tag"]
  :adapter {:integration :local/db}
  :audit {:records [:tag/change]
          :fields [:tag/id :tag/name]}
  :idempotency {:strategy :dedupe-on
                :keys [:tag/name]}
  :contracts {:fixtures [:fixtures/tags]}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/tag]
            :views [:cap/view/tasks]}}

 {:id :cap/action/tag-update
  :title "Rename Tag"
  :version "v1"
  :inputs {:tag/id :uuid
           :tag/name :string}
  :outputs {:tag/id :uuid
            :tag/name :string}
  :side-effects ["Persist tag name change"]
  :adapter {:integration :local/db}
  :audit {:records [:tag/change]
          :fields [:tag/id :tag/name]}
  :idempotency {:strategy :dedupe-on
                :keys [:tag/id]}
  :contracts {:fixtures [:fixtures/tags]}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/tag]
            :views [:cap/view/tasks]}}

 {:id :cap/action/tag-delete
  :title "Delete Tag"
  :version "v1"
  :inputs {:tag/id :uuid}
  :outputs {:tag/id :uuid}
  :side-effects ["Remove tag from tasks and delete entity"]
  :adapter {:integration :local/db}
  :audit {:records [:tag/change]
          :fields [:tag/id]}
  :idempotency {:strategy :dedupe-on
                :keys [:tag/id]}
  :contracts {:fixtures [:fixtures/tags]}
 :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/tag]
            :views [:cap/view/tasks]}}

 {:id :cap/action/task-delete
  :title "Delete Task"
  :version "v1"
  :inputs {:task/id :uuid}
  :outputs {:task/id :uuid}
  :side-effects ["Delete task entity and detach tag refs"]
  :adapter {:integration :local/db}
  :audit {:records [:task/change]
          :fields [:task/id]}
  :idempotency {:strategy :dedupe-on
                :keys [:task/id]}
  :contracts {:fixtures [:fixtures/tasks]}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/task]
            :views [:cap/view/tasks]}
  :tooling [:cap/tooling/action-contract]}

 {:id :cap/action/task-read
  :title "Read Task"
  :version "v1"
  :inputs {:task/id :uuid}
  :outputs {:task/id :uuid}
  :side-effects ["Read task entity"]
  :adapter {:integration :local/db}
  :audit {:records [:task/read]
          :fields [:task/id]}
  :idempotency {:strategy :pure}
  :contracts {:fixtures [:fixtures/tasks]}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/task]
            :views [:cap/view/tasks]}
  :tooling []}

 {:id :cap/action/parcel-import
  :title "Import land registry CSV"
  :version "v1"
  :inputs {:file/path :string
           :mode :keyword
           :dry-run? :boolean?}
  :outputs {:import/persons :long
            :import/parcels :long
            :import/ownerships :long}
  :side-effects ["Transact person/parcel/ownership entities into Datomic"]
  :adapter {:integration :local/db}
  :audit {:records [:import/run]
          :fields [:file/path]}
  :idempotency {:strategy :deterministic-ids
                :keys [:file/path]}
 :contracts {:fixtures [:fixtures/land-registry-sample]}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/person :cap/schema/parcel :cap/schema/ownership]
            :views [:cap/view/land-registry]}
  :tooling [:cap/tooling/import-check]}

 {:id :cap/action/person-list
  :title "List people (land registry)"
  :version "v1"
  :inputs {:q :string? :sort :keyword? :limit :long? :offset :long?}
  :outputs {:people :vector
            :pagination {:total :long
                         :limit :long
                         :offset :long}}
  :side-effects []
  :adapter {:integration :local/db}
  :audit {:records [] :fields []}
  :idempotency {:strategy :pure}
  :contracts {:fixtures [:fixtures/land-registry-sample]}
  :compatibility {:backward true :forward true :flags []}
  :related {:schema [:cap/schema/person :cap/schema/ownership]
            :views [:cap/view/land-registry]}
  :tooling []}

 {:id :cap/action/person-detail
  :title "Person detail (land registry)"
  :version "v1"
  :inputs {:person/id :uuid}
  :outputs {:person/id :uuid}
  :side-effects []
  :adapter {:integration :local/db}
  :audit {:records [] :fields []}
  :idempotency {:strategy :pure}
  :contracts {:fixtures [:fixtures/land-registry-sample]}
  :compatibility {:backward true :forward true :flags []}
  :related {:schema [:cap/schema/person :cap/schema/ownership :cap/schema/parcel]
            :views [:cap/view/land-registry]}
  :tooling []}

 {:id :cap/action/parcel-list
  :title "List parcels"
  :version "v1"
  :inputs {:cadastral-id :string? :parcel-number :string? :min-area :string? :max-area :string? :completeness :keyword? :sort :keyword? :limit :long? :offset :long?}
  :outputs {:parcels :vector
            :pagination {:total :long
                         :limit :long
                         :offset :long}}
  :side-effects []
  :adapter {:integration :local/db}
  :audit {:records [] :fields []}
  :idempotency {:strategy :pure}
  :contracts {:fixtures [:fixtures/land-registry-sample]}
  :compatibility {:backward true :forward true :flags []}
  :related {:schema [:cap/schema/parcel :cap/schema/ownership :cap/schema/person]
            :views [:cap/view/land-registry]}
  :tooling []}

 {:id :cap/action/parcel-detail
  :title "Parcel detail"
  :version "v1"
  :inputs {:parcel/id :uuid}
  :outputs {:parcel/id :uuid}
  :side-effects []
  :adapter {:integration :local/db}
  :audit {:records [] :fields []}
  :idempotency {:strategy :pure}
  :contracts {:fixtures [:fixtures/land-registry-sample]}
  :compatibility {:backward true :forward true :flags []}
  :related {:schema [:cap/schema/parcel :cap/schema/ownership :cap/schema/person]
            :views [:cap/view/land-registry]}
  :tooling []}

 {:id :cap/action/parcel-stats
  :title "Parcel/person summary stats"
  :version "v1"
  :inputs {}
  :outputs {:persons :long :parcels :long :total-area-m2 :double}
  :side-effects []
  :adapter {:integration :local/db}
  :audit {:records [] :fields []}
  :idempotency {:strategy :pure}
  :contracts {:fixtures [:fixtures/land-registry-sample]}
  :compatibility {:backward true :forward true :flags []}
  :related {:schema [:cap/schema/parcel :cap/schema/person :cap/schema/ownership]
            :views [:cap/view/land-registry]}
  :tooling []}

 {:id :cap/action/content-blocks
  :title "Content Blocks CRUD"
  :version "v1"
  :inputs {:content.block/id :uuid?
           :content.block/page :uuid?
           :content.block/type :keyword?
           :content.block/title :string?
           :content.block/body :string?
           :content.block/media-ref :string?
           :content.block/slug :string?
           :content.block/order :long?
           :content.block/visible? :boolean?
           :content.block/tag :uuid-set?}
  :outputs {:content.block {:content.block/id :uuid
                            :content.block/type :keyword
                            :content.block/slug :string}}
  :side-effects []
  :adapter {:integration :local/db}
  :audit {:records [:content/change] :fields [:content.block/id :content.block/type]}
  :idempotency {:strategy :dedupe-on :keys [:content.block/id]}
  :contracts {:fixtures [:fixtures/content]}
  :compatibility {:backward true :forward true :flags []}
  :related {:schema [:cap/schema/content-block :cap/schema/content-tag]
            :views [:cap/view/control-panel]}
  :tooling []}

 {:id :cap/action/content-pages
  :title "Content Pages CRUD"
  :version "v1"
  :inputs {:content.page/id :uuid?
           :content.page/title :string?
           :content.page/path :string?
           :content.page/summary :string?
           :content.page/navigation-order :long?
           :content.page/blocks :uuid-set?
           :content.page/visible? :boolean?
           :content.page/tag :uuid-set?}
  :outputs {:content.page {:content.page/id :uuid
                           :content.page/path :string}}
  :side-effects []
  :adapter {:integration :local/db}
  :audit {:records [:content/change] :fields [:content.page/id :content.page/path]}
  :idempotency {:strategy :dedupe-on :keys [:content.page/id]}
  :contracts {:fixtures [:fixtures/content]}
  :compatibility {:backward true :forward true :flags []}
  :related {:schema [:cap/schema/content-page :cap/schema/content-block :cap/schema/content-tag]
            :views [:cap/view/control-panel]}
  :tooling []}

 {:id :cap/action/content-tags
  :title "Content Tags CRUD"
  :version "v1"
  :inputs {:content.tag/id :uuid?
           :content.tag/name :string?
           :content.tag/slug :string?
           :content.tag/description :string?}
  :outputs {:content.tag {:content.tag/id :uuid
                          :content.tag/name :string}}
  :side-effects []
  :adapter {:integration :local/db}
  :audit {:records [:content/change] :fields [:content.tag/id :content.tag/name]}
  :idempotency {:strategy :dedupe-on :keys [:content.tag/id :content.tag/name]}
  :contracts {:fixtures [:fixtures/content]}
  :compatibility {:backward true :forward true :flags []}
  :related {:schema [:cap/schema/content-tag]
            :views [:cap/view/control-panel]}
  :tooling []}

 {:id :cap/action/content-v2
  :title "Content V2 Read"
  :version "v1"
  :purpose "List Saudi license site content entities for control panel/public site rendering."
  :inputs {}
  :outputs {:content/v2 {:licenses :vector
                         :comparison-rows :vector
                         :journey-phases :vector
                         :activation-steps :vector
                         :personas :vector
                         :support-entries :vector
                         :hero-stats :vector
                         :hero-flows :vector
                         :faqs :vector
                         :values :vector
                         :team-members :vector
                         :contacts :vector
                         :businesses :vector}}
  :side-effects {:http/endpoints ["/api/content/v2"]}
  :adapter {:integration :local/db}
  :audit {:records [:content/read] :fields []}
  :idempotency {:strategy :pure}
  :contracts {:fixtures [:fixtures/content]}
 :compatibility {:backward true :forward true :flags []}
 :related {:schema [:cap/schema/license
                     :cap/schema/comparison-row
                     :cap/schema/journey-phase
                     :cap/schema/activation-step
                     :cap/schema/persona
                     :cap/schema/support-responsibility
                     :cap/schema/hero-stat
                     :cap/schema/hero-flow
                     :cap/schema/faq
                     :cap/schema/value
                     :cap/schema/team-member
                     :cap/schema/business
                     :cap/schema/contact]
            :views [:cap/view/control-panel]}
  :tooling []}

 {:id :cap/action/contact-upsert
  :title "Contact Upsert"
  :version "v1"
  :purpose "Create/update contact entities (email, phone, CTA labels/URLs) for public site and control panel."
  :inputs {:contact/id :uuid?
           :contact/email :string
           :contact/phone :string
           :contact/primary-cta-label :string?
           :contact/primary-cta-url :string?
           :contact/secondary-cta-label :string?
           :contact/secondary-cta-url :string?}
  :outputs {:contact {:contact/id :uuid}}
  :side-effects {:http/endpoints ["/api/content/contacts" "/api/content/contacts/:id"]}
  :adapter {:integration :local/db}
  :audit {:records [:content/change] :fields [:contact/id :contact/email]}
  :idempotency {:strategy :dedupe-on :keys [:contact/id :contact/email]}
  :contracts {:fixtures [:fixtures/content]}
  :compatibility {:backward true :forward true :flags []}
  :related {:schema [:cap/schema/contact]
            :views [:cap/view/control-panel :cap/view/site-contact :cap/view/site-home :cap/view/site-services :cap/view/site-about]}
  :tooling []}

 {:id :cap/action/business-upsert
  :title "Business Upsert"
  :version "v1"
  :purpose "Create/update business entity (nav label, hero headline/strapline, contact ref, hero stats/flows, visibility)."
  :inputs {:business/id :uuid?
           :business/name :string
           :business/tagline :string?
           :business/summary :string?
           :business/mission :string?
           :business/vision :string?
           :business/nav-label :string?
           :business/hero-headline :string?
           :business/hero-strapline :string?
           :business/contact :uuid?
           :business/hero-stats [:uuid]
           :business/hero-flows [:uuid]
           :business/visible? :boolean?}
  :outputs {:business {:business/id :uuid}}
  :side-effects {:http/endpoints ["/api/content/businesses" "/api/content/businesses/:id"]}
  :adapter {:integration :local/db}
  :audit {:records [:content/change] :fields [:business/id :business/name]}
  :idempotency {:strategy :dedupe-on :keys [:business/id :business/name]}
  :contracts {:fixtures [:fixtures/content]}
  :compatibility {:backward true :forward true :flags []}
  :related {:schema [:cap/schema/business :cap/schema/contact :cap/schema/hero-stat :cap/schema/hero-flow]
            :views [:cap/view/control-panel :cap/view/site-home :cap/view/site-services :cap/view/site-about :cap/view/site-contact]}
  :tooling []}

 {:id :cap/action/license-upsert
  :title "License Upsert"
  :version "v1"
  :purpose "Create/update license content entities."
  :inputs {:license/id :uuid?
           :license/type :keyword
           :license/slug :string
           :license/label :string
           :license/processing-time :string?
           :license/ownership :string?
           :license/renewal-cost :string?
           :license/pricing-lines :string-seq?
           :license/activities :string-seq?
           :license/who :string-seq?
           :license/who-activities :string-seq?
           :license/document-checklist :string-seq?
           :license/order :long?
           :license/visible? :boolean?}
  :outputs {:license {:license/id :uuid
                      :license/slug :string}}
  :side-effects {:http/endpoints ["/api/content/licenses" "/api/content/licenses/:id"]
                 :audit [:content/change]}
  :adapter {:integration :local/db}
  :audit {:records [:content/change] :fields [:license/id :license/type :license/slug]}
  :idempotency {:strategy :dedupe-on :keys [:license/id :license/slug]}
  :contracts {:fixtures [:fixtures/content]}
  :compatibility {:backward true :forward true :flags []}
  :related {:schema [:cap/schema/license]
            :views [:cap/view/control-panel]}
  :tooling []}

 {:id :cap/action/license-delete
  :title "License Delete"
  :version "v1"
  :purpose "Delete a license entity by id."
  :inputs {:license/id :uuid}
  :outputs {:status :deleted}
  :side-effects {:http/endpoints ["/api/content/licenses/:id"]
                 :audit [:content/change]}
  :adapter {:integration :local/db}
  :audit {:records [:content/change] :fields [:license/id]}
  :idempotency {:strategy :dedupe-on :keys [:license/id]}
  :contracts {:fixtures [:fixtures/content]}
  :compatibility {:backward true :forward true :flags []}
  :related {:schema [:cap/schema/license]
            :views [:cap/view/control-panel]}
  :tooling []}

 {:id :cap/action/comparison-row-upsert
  :title "Comparison Row Upsert"
  :version "v1"
  :purpose "Create/update comparison rows for license matrices."
  :inputs {:comparison.row/id :uuid?
           :comparison.row/criterion :string
           :comparison.row/order :long?
           :comparison.row/entrepreneur :string?
           :comparison.row/general :string?
           :comparison.row/gcc :string?}
  :outputs {:comparison.row {:comparison.row/id :uuid}}
  :side-effects {:http/endpoints ["/api/content/comparison-rows" "/api/content/comparison-rows/:id"]
                 :audit [:content/change]}
  :adapter {:integration :local/db}
  :audit {:records [:content/change] :fields [:comparison.row/id :comparison.row/criterion]}
 :idempotency {:strategy :dedupe-on :keys [:comparison.row/id :comparison.row/criterion]}
 :contracts {:fixtures [:fixtures/content]}
 :compatibility {:backward true :forward true :flags []}
 :related {:schema [:cap/schema/comparison-row]
           :views [:cap/view/control-panel]}
 :tooling []}

 {:id :cap/action/journey-phase-upsert
  :title "Journey Phase Upsert"
  :version "v1"
  :purpose "Create/update journey phases."
  :inputs {:journey.phase/id :uuid?
           :journey.phase/title :string
           :journey.phase/kind :keyword
           :journey.phase/order :long?
           :journey.phase/bullets :string-seq?}
  :outputs {:journey.phase {:journey.phase/id :uuid}}
  :side-effects {:http/endpoints ["/api/content/journey-phases" "/api/content/journey-phases/:id"]}
  :adapter {:integration :local/db}
  :audit {:records [:content/change] :fields [:journey.phase/id :journey.phase/title]}
  :idempotency {:strategy :dedupe-on :keys [:journey.phase/id]}
  :contracts {:fixtures [:fixtures/content]}
  :compatibility {:backward true :forward true :flags []}
  :related {:schema [:cap/schema/journey-phase]
            :views [:cap/view/control-panel]}
  :tooling []}

 {:id :cap/action/journey-phase-delete
  :title "Journey Phase Delete"
  :version "v1"
  :purpose "Delete a journey phase."
  :inputs {:journey.phase/id :uuid}
  :outputs {:status :deleted}
  :side-effects {:http/endpoints ["/api/content/journey-phases/:id"]}
  :adapter {:integration :local/db}
  :audit {:records [:content/change] :fields [:journey.phase/id]}
  :idempotency {:strategy :dedupe-on :keys [:journey.phase/id]}
  :contracts {:fixtures [:fixtures/content]}
  :compatibility {:backward true :forward true :flags []}
  :related {:schema [:cap/schema/journey-phase]
            :views [:cap/view/control-panel]}
  :tooling []}

 {:id :cap/action/activation-step-upsert
  :title "Activation Step Upsert"
  :version "v1"
  :purpose "Create/update activation steps."
  :inputs {:activation.step/id :uuid?
           :activation.step/title :string
           :activation.step/order :long?
           :activation.step/phase :uuid?}
  :outputs {:activation.step {:activation.step/id :uuid}}
  :side-effects {:http/endpoints ["/api/content/activation-steps" "/api/content/activation-steps/:id"]}
  :adapter {:integration :local/db}
  :audit {:records [:content/change] :fields [:activation.step/id :activation.step/title]}
  :idempotency {:strategy :dedupe-on :keys [:activation.step/id]}
  :contracts {:fixtures [:fixtures/content]}
  :compatibility {:backward true :forward true :flags []}
  :related {:schema [:cap/schema/activation-step]
            :views [:cap/view/control-panel]}
  :tooling []}

 {:id :cap/action/activation-step-delete
  :title "Activation Step Delete"
  :version "v1"
  :purpose "Delete an activation step."
  :inputs {:activation.step/id :uuid}
  :outputs {:status :deleted}
  :side-effects {:http/endpoints ["/api/content/activation-steps/:id"]}
  :adapter {:integration :local/db}
  :audit {:records [:content/change] :fields [:activation.step/id]}
  :idempotency {:strategy :dedupe-on :keys [:activation.step/id]}
  :contracts {:fixtures [:fixtures/content]}
  :compatibility {:backward true :forward true :flags []}
  :related {:schema [:cap/schema/activation-step]
            :views [:cap/view/control-panel]}
  :tooling []}

 {:id :cap/action/comparison-row-delete
  :title "Comparison Row Delete"
  :version "v1"
  :purpose "Delete a comparison row by id."
  :inputs {:comparison.row/id :uuid}
  :outputs {:status :deleted}
  :side-effects {:http/endpoints ["/api/content/comparison-rows/:id"]
                 :audit [:content/change]}
  :adapter {:integration :local/db}
  :audit {:records [:content/change] :fields [:comparison.row/id]}
  :idempotency {:strategy :dedupe-on :keys [:comparison.row/id]}
  :contracts {:fixtures [:fixtures/content]}
 :compatibility {:backward true :forward true :flags []}
  :related {:schema [:cap/schema/comparison-row]
            :views [:cap/view/control-panel]}
 :tooling []}

 {:id :cap/action/telegram-send-message
  :title "Telegram Send Message"
  :version "v1"
  :inputs {:telegram/chat-id :string
           :telegram/text :string
           :telegram/parse-mode :keyword?
           :telegram/message-key :string}
  :outputs {:telegram/message-id :string}
  :side-effects ["Send Telegram message via Bot API"]
  :adapter {:integration :cap/integration/telegram-bot}
  :audit {:records [:telegram/send]
          :fields [:telegram/chat-id :telegram/message-key]}
  :idempotency {:strategy :dedupe-on
                :keys [:telegram/chat-id :telegram/message-key]}
  :contracts {:fixtures []}
  :compatibility {:backward true :forward true :flags [:telegram/notifications-enabled?]}
  :related {:schema [:cap/schema/user]
            :views []}
  :tooling []}

 {:id :cap/action/telegram-set-webhook
  :title "Telegram Set Webhook"
  :version "v1"
  :inputs {:telegram/webhook-url :string
           :telegram/secret-token :string}
  :outputs {:status :ok}
  :side-effects ["Register webhook URL and secret with Telegram"]
  :adapter {:integration :cap/integration/telegram-bot}
  :audit {:records [:telegram/webhook-set]
          :fields [:telegram/webhook-url]}
  :idempotency {:strategy :dedupe-on
                :keys [:telegram/webhook-url :telegram/secret-token]}
  :contracts {:fixtures []}
  :compatibility {:backward true :forward true :flags [:telegram/webhook-enabled?]}
  :related {:schema [] :views []}
  :tooling []}

 {:id :cap/action/telegram-handle-update
  :title "Telegram Handle Update"
  :version "v1"
  :inputs {:telegram/update-id :long
           :telegram/chat-id :string
           :telegram/message-text :string?
           :telegram/command :keyword?}
  :outputs {:status :handled
            :telegram/command :keyword?}
  :side-effects ["Dispatch Telegram commands and emit replies"
                 "Persist chat-id mapping via link token when applicable"]
  :adapter {:integration :cap/integration/telegram-bot}
  :audit {:records [:telegram/update]
          :fields [:telegram/update-id :telegram/chat-id :telegram/command]}
  :idempotency {:strategy :dedupe-on
                :keys [:telegram/update-id]}
 :contracts {:fixtures []}
 :compatibility {:backward true :forward true :flags [:telegram/webhook-enabled? :telegram/commands-enabled?]}
  :related {:schema [:cap/schema/user]
            :views []}
  :tooling []}

 {:id :cap/action/betting-events
  :title "Betting Events (Rezultati)"
  :version "v1"
  :inputs {:day-offset :long?
           :sport-path :string?
           :refresh? :boolean?}
  :outputs {:events :list}
  :side-effects ["Fetch Rezultati odds list" "Upsert betting events"]
  :adapter {:integration :cap/integration/rezultati}
  :audit {:records [:betting/events]}
  :idempotency {:strategy :none}
  :contracts {:fixtures [:fixtures/betting]}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/betting-event]
            :views [:cap/view/betting]}
  :tooling []}

 {:id :cap/action/betting-odds
  :title "Betting Odds Snapshot"
  :version "v1"
 :inputs {:betting.event/id :uuid
           :sport-path :string?
           :refresh? :boolean?}
 :outputs {:quotes :list}
  :side-effects ["Fetch Rezultati match odds" "Fetch execution book odds" "Store quote snapshots"]
  :adapter {:integration :cap/integration/rezultati}
  :audit {:records [:betting/quote-capture]
          :fields [:betting.event/id]}
  :idempotency {:strategy :none}
  :contracts {:fixtures [:fixtures/betting]}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/betting-event :cap/schema/betting-quote]
            :views [:cap/view/betting]}
  :tooling []}

 {:id :cap/action/betting-bet-log
  :title "Betting Log Bet"
  :version "v1"
 :inputs {:betting.event/id :uuid
           :betting.bet/market-key :string?
           :betting.bet/selection :string
           :betting.bet/odds-decimal :double?
           :betting.bookmaker/key :string?}
  :outputs {:betting.bet/id :uuid}
  :side-effects ["Persist bet and fact"]
  :adapter {:integration :local/db}
  :audit {:records [:betting/bet-log]
          :fields [:betting.event/id :betting.bet/selection]}
  :idempotency {:strategy :none}
  :contracts {:fixtures [:fixtures/betting]}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/betting-bet :cap/schema/betting-fact]
            :views [:cap/view/betting]}
  :tooling []}

 {:id :cap/action/betting-close
  :title "Betting Capture Close"
  :version "v1"
 :inputs {:betting.event/id :uuid
           :sport-path :string?
           :refresh? :boolean?}
 :outputs {:quotes :list}
  :side-effects ["Fetch Rezultati match odds" "Fetch execution book odds" "Store close snapshots"]
  :adapter {:integration :cap/integration/rezultati}
  :audit {:records [:betting/close-capture]
          :fields [:betting.event/id]}
  :idempotency {:strategy :none}
  :contracts {:fixtures [:fixtures/betting]}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/betting-event :cap/schema/betting-quote]
            :views [:cap/view/betting]}
  :tooling []}

 {:id :cap/action/betting-settle
  :title "Betting Settle Bet"
  :version "v1"
  :inputs {:betting.bet/id :uuid
           :betting.bet/status :keyword}
  :outputs {:betting.bet/id :uuid}
  :side-effects ["Persist bet settlement and fact"]
  :adapter {:integration :local/db}
  :audit {:records [:betting/settle]
          :fields [:betting.bet/id :betting.bet/status]}
  :idempotency {:strategy :none}
  :contracts {:fixtures [:fixtures/betting]}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/betting-bet :cap/schema/betting-fact]
            :views [:cap/view/betting]}
  :tooling []}

 {:id :cap/action/file-upload
  :title "Upload File"
  :version "v1"
  :inputs {:file/upload :binary
           :file/filename :string?
           :file/mime :string?
           :file/content-base64 :string?
           :file/slug :string?}
  :outputs {:file/id :uuid
            :file/slug :string
            :file/type :keyword}
  :side-effects ["Store file on local disk" "Persist file metadata in Datomic"]
  :adapter {:integration :cap/integration/local-file-store}
  :audit {:records [:file/change]
          :fields [:file/id :file/slug :file/type]}
  :idempotency {:strategy :none}
  :contracts {:fixtures []}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/file :cap/schema/user]
            :views [:cap/view/file-library]}
  :tooling []}

 {:id :cap/action/file-delete
  :title "Delete File"
  :version "v1"
  :inputs {:file/id :uuid}
  :outputs {:file/id :uuid}
  :side-effects ["Delete file from disk" "Remove metadata from Datomic"]
  :adapter {:integration :cap/integration/local-file-store}
  :audit {:records [:file/change]
          :fields [:file/id]}
  :idempotency {:strategy :dedupe-on
                :keys [:file/id]}
  :contracts {:fixtures []}
 :compatibility {:backward true
                 :forward true
                 :flags []}
  :related {:schema [:cap/schema/file]
            :views [:cap/view/file-library]}
  :tooling []}

 {:id :cap/action/site-screenshot-bundle
  :title "Capture Site Screenshot Bundle"
  :version "v1"
  :inputs {:bundle/title :string?
           :bundle/type :keyword?
           :pages :vector
           :viewports :vector}
  :outputs {:bundle/id :uuid
            :bundle/slug :string
            :bundle/count :long}
  :side-effects ["Capture screenshots via Playwright"
                 "Store images in file library"
                 "Persist bundle metadata in Datomic"]
  :adapter {:integration :cap/integration/local-file-store}
  :audit {:records [:bundle/change]
          :fields [:bundle/id :bundle/type]}
  :idempotency {:strategy :none}
  :contracts {:fixtures []}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/file :cap/schema/library-bundle]
            :views [:cap/view/home :cap/view/file-library]}
  :tooling []}

 {:id :cap/action/file-read
  :title "Read File"
  :version "v1"
  :inputs {:file/id :uuid}
  :outputs {:file/id :uuid}
  :side-effects ["Read file metadata"]
  :adapter {:integration :cap/integration/local-file-store}
  :audit {:records [:file/read]
          :fields [:file/id]}
  :idempotency {:strategy :pure}
  :contracts {:fixtures []}
  :compatibility {:backward true
                  :forward true
                  :flags []}
 :related {:schema [:cap/schema/file]
            :views [:cap/view/file-library]}
  :tooling []}

 {:id :cap/action/account-statement-generate
  :title "Generate Account Statement"
  :version "v1"
  :purpose "Render a professional Account Statement PDF and store it in the file library."
  :inputs {:statement/company-name :string
           :statement/date :string?
           :statement/client-name :string
           :statement/currency :string?
           :statement/total-contract-amount :number?
           :statement/total-amount-received :number?
           :statement/outstanding-balance :number?
           :statement/payments :vector?
           :statement/remarks :string?
           :statement/slug :string?}
  :outputs {:file/id :uuid
            :file/slug :string?
            :file/url :string}
  :side-effects ["Render PDF via Playwright"
                 "Store PDF in file library"]
  :adapter {:integration :cap/integration/local-file-store
            :renderer :cap/tooling/account-statement-pdf}
  :audit {:records [:statement/generate]
          :fields [:statement/client-name :statement/date :file/slug]}
  :idempotency {:strategy :none}
  :contracts {:fixtures []}
  :compatibility {:backward true
                  :forward true
                  :flags []}
 :related {:schema [:cap/schema/file]
            :views [:cap/view/file-library]}
  :tooling [:cap/tooling/account-statement-pdf]}

 {:id :cap/action/doc-pack-upsert
  :title "Doc Pack Upsert"
  :version "v1"
  :purpose "Create/update a client document pack (company info, services, payment plan, status notes)."
  :inputs {:client/id :uuid
           :doc.pack/company-name :string?
           :doc.pack/company-address :string?
           :doc.pack/company-email :string?
           :doc.pack/company-phone :string?
           :doc.pack/currency :string?
           :doc.pack/services-included :string?
           :doc.pack/payment-plan :string?
           :doc.pack/status-notes :string?}
  :outputs {:doc-pack :map}
  :side-effects ["Persist doc pack"]
  :adapter {:integration :local/db}
  :audit {:records [:doc-pack/change]
          :fields [:client/id]}
  :idempotency {:strategy :dedupe-on
                :keys [:client/id]}
  :contracts {:fixtures []}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/doc-pack :cap/schema/client]
            :views [:cap/view/clients]}
  :tooling []}

 {:id :cap/action/doc-pack-read
  :title "Doc Pack Read"
  :version "v1"
  :purpose "Read a client doc pack and its invoices/payments."
  :inputs {:client/id :uuid}
  :outputs {:doc-pack :map
            :invoices :vector
            :payments :vector}
  :side-effects []
  :adapter {:integration :local/db}
  :audit {:records [:doc-pack/read]
          :fields [:client/id]}
  :idempotency {:strategy :pure}
  :contracts {:fixtures []}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/doc-pack :cap/schema/invoice :cap/schema/payment]
            :views [:cap/view/clients]}
  :tooling []}

 {:id :cap/action/agreement-create
  :title "Agreement Create"
  :version "v1"
  :purpose "Create an agreement for a client (terms + delivery/consent metadata)."
  :inputs {:client/id :uuid
           :agreement/number :string?
           :agreement/title :string
           :agreement/terms :string
           :agreement/effective-at :instant?
           :agreement/client-company :string?
           :agreement/client-representative :string?
           :agreement/our-representative :string?
           :agreement/our-recipient :string?
           :agreement/delivery-channels [:set :keyword]
           :agreement/delivery-email :string?
           :agreement/delivery-phone :string?
           :agreement/delivery-telegram-chat-id :string?
           :agreement/electronic-consent? :boolean?
           :agreement/consented-at :instant?}
  :outputs {:agreement :map}
  :side-effects ["Persist agreement"]
  :adapter {:integration :local/db}
  :audit {:records [:agreement/change]
          :fields [:client/id :agreement/number]}
  :idempotency {:strategy :none}
  :contracts {:fixtures []}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/agreement :cap/schema/client]
            :views [:cap/view/clients]}
  :tooling []}

 {:id :cap/action/agreement-update
  :title "Agreement Update"
  :version "v1"
  :purpose "Update an agreement (title/terms/delivery/consent fields)."
  :inputs {:agreement/id :uuid
           :agreement/number :string?
           :agreement/title :string?
           :agreement/terms :string?
           :agreement/effective-at :instant?
           :agreement/client-company :string?
           :agreement/client-representative :string?
           :agreement/our-representative :string?
           :agreement/our-recipient :string?
           :agreement/delivery-channels [:set :keyword]
           :agreement/delivery-email :string?
           :agreement/delivery-phone :string?
           :agreement/delivery-telegram-chat-id :string?
           :agreement/electronic-consent? :boolean?
           :agreement/consented-at :instant?
           :agreement/status :keyword?}
  :outputs {:agreement :map}
  :side-effects ["Persist agreement updates"]
  :adapter {:integration :local/db}
  :audit {:records [:agreement/change]
          :fields [:agreement/id]}
  :idempotency {:strategy :dedupe-on
                :keys [:agreement/id]}
  :contracts {:fixtures []}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/agreement]
            :views [:cap/view/clients]}
  :tooling []}

 {:id :cap/action/agreement-list
  :title "Agreement List"
  :version "v1"
  :purpose "List agreements for a client."
  :inputs {:client/id :uuid}
  :outputs {:agreements :vector}
  :side-effects []
  :adapter {:integration :local/db}
  :audit {:records [:agreement/read]
          :fields [:client/id]}
  :idempotency {:strategy :pure}
  :contracts {:fixtures []}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/agreement :cap/schema/client]
            :views [:cap/view/clients]}
  :tooling []}

 {:id :cap/action/agreement-propose
  :title "Agreement Propose"
  :version "v1"
  :purpose "Mark an agreement as proposed (proposal issued) and issue an immutable proposal PDF for that agreement draft."
  :inputs {:agreement/id :uuid
           :agreement/proposed-at :instant?
           :agreement/proposed-by :string?}
  :outputs {:agreement :map
            :proposal-pdf :map}
  :side-effects ["Persist agreement status transition to :proposed"
                 "Auto-issue (or reuse) immutable proposal document for the agreement"]
  :adapter {:integration :local/db}
  :audit {:records [:agreement/change]
          :fields [:agreement/id :agreement/proposed-at]}
  :idempotency {:strategy :dedupe-on
                :keys [:agreement/id :agreement/proposed-at]}
  :contracts {:fixtures []}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/agreement :cap/schema/payment-plan-item :cap/schema/document]
            :views [:cap/view/clients]}
  :tooling []}

 {:id :cap/action/agreement-accept
  :title "Agreement Accept"
  :version "v1"
  :purpose "Mark an agreement as accepted and auto-generate invoices for plan items configured for acceptance."
  :inputs {:agreement/id :uuid
           :agreement/accepted-at :instant?
           :agreement/accepted-by :string?
           :agreement/delivery-channels [:set :keyword]
           :agreement/delivery-email :string?
           :agreement/delivery-phone :string?
           :agreement/delivery-telegram-chat-id :string?
           :agreement/electronic-consent? :boolean?
           :agreement/consented-at :instant?}
  :outputs {:agreement :map
            :invoices :vector}
  :side-effects ["Persist agreement acceptance"
                 "Auto-generate invoices from structured payment plan items"
                 "Auto-issue (or reuse) immutable invoice documents for generated invoices"]
  :adapter {:integration :local/db}
  :audit {:records [:agreement/change]
          :fields [:agreement/id]}
  :idempotency {:strategy :dedupe-on
                :keys [:agreement/id :agreement/accepted-at]}
  :contracts {:fixtures []}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/agreement :cap/schema/payment-plan-item :cap/schema/invoice :cap/schema/document]
            :views [:cap/view/clients]}
  :tooling []}

 {:id :cap/action/plan-item-create
  :title "Plan Item Create"
  :version "v1"
  :purpose "Add a structured payment plan item to an agreement."
  :inputs {:agreement/id :uuid
           :plan.item/kind :keyword
           :plan.item/label :string
           :plan.item/amount :number
           :plan.item/currency :string?
           :plan.item/due-at :instant
           :plan.item/invoice-on :keyword
           :plan.item/invoice-status :keyword?
           :plan.item/index :long?
           :plan.item/active? :boolean?
           :plan.item/recurrence :string?}
  :outputs {:plan-item :map}
  :side-effects ["Persist plan item"]
  :adapter {:integration :local/db}
  :audit {:records [:agreement/change]
          :fields [:agreement/id :plan.item/label]}
  :idempotency {:strategy :none}
  :contracts {:fixtures []}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/payment-plan-item :cap/schema/agreement]
            :views [:cap/view/clients]}
  :tooling []}

 {:id :cap/action/plan-item-update
  :title "Plan Item Update"
  :version "v1"
  :purpose "Update a structured payment plan item."
  :inputs {:plan.item/id :uuid
           :plan.item/kind :keyword?
           :plan.item/label :string?
           :plan.item/amount :number?
           :plan.item/currency :string?
           :plan.item/due-at :instant?
           :plan.item/invoice-on :keyword?
           :plan.item/invoice-status :keyword?
           :plan.item/index :long?
           :plan.item/active? :boolean?
           :plan.item/recurrence :string?}
  :outputs {:plan-item :map}
  :side-effects ["Persist plan item updates"]
  :adapter {:integration :local/db}
  :audit {:records [:agreement/change]
          :fields [:plan.item/id]}
  :idempotency {:strategy :dedupe-on
                :keys [:plan.item/id]}
  :contracts {:fixtures []}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/payment-plan-item]
            :views [:cap/view/clients]}
  :tooling []}

 {:id :cap/action/plan-item-delete
  :title "Plan Item Delete"
  :version "v1"
  :purpose "Deactivate (soft-delete) a plan item."
  :inputs {:plan.item/id :uuid}
  :outputs {:plan.item/id :uuid}
  :side-effects ["Deactivate plan item"]
  :adapter {:integration :local/db}
  :audit {:records [:agreement/change]
          :fields [:plan.item/id]}
  :idempotency {:strategy :dedupe-on
                :keys [:plan.item/id]}
  :contracts {:fixtures []}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/payment-plan-item]
            :views [:cap/view/clients]}
  :tooling []}

 {:id :cap/action/plan-item-list
  :title "Plan Item List"
  :version "v1"
  :purpose "List plan items for an agreement."
  :inputs {:agreement/id :uuid}
  :outputs {:plan-items :vector}
  :side-effects []
  :adapter {:integration :local/db}
  :audit {:records [:agreement/read]
          :fields [:agreement/id]}
  :idempotency {:strategy :pure}
  :contracts {:fixtures []}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/payment-plan-item :cap/schema/agreement]
            :views [:cap/view/clients]}
  :tooling []}

 {:id :cap/action/plan-item-invoice-ensure
  :title "Plan Item Invoice Ensure"
  :version "v1"
  :purpose "Ensure a plan item has an invoice (create if missing)."
  :inputs {:agreement/id :uuid
           :plan.item/id :uuid}
  :outputs {:invoice :map}
  :side-effects ["Create missing invoice for plan item"]
  :adapter {:integration :local/db}
  :audit {:records [:invoice/change]
          :fields [:agreement/id :plan.item/id]}
  :idempotency {:strategy :dedupe-on
                :keys [:agreement/id :plan.item/id]}
  :contracts {:fixtures []}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/payment-plan-item :cap/schema/agreement :cap/schema/invoice]
            :views [:cap/view/clients]}
  :tooling []}

 {:id :cap/action/plan-item-invoice-issue
  :title "Plan Item Invoice Issue"
  :version "v1"
  :purpose "Ensure an invoice exists for a plan item and issue (or reuse) the immutable invoice PDF."
  :inputs {:agreement/id :uuid
           :plan.item/id :uuid}
  :outputs {:invoice :map
            :invoice-pdf :map}
  :side-effects ["Create missing invoice for plan item"
                 "Auto-issue (or reuse) immutable invoice document"]
  :adapter {:integration :local/db}
  :audit {:records [:invoice/change]
          :fields [:agreement/id :plan.item/id]}
  :idempotency {:strategy :dedupe-on
                :keys [:agreement/id :plan.item/id]}
  :contracts {:fixtures []}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/payment-plan-item :cap/schema/agreement :cap/schema/invoice :cap/schema/document]
            :views [:cap/view/clients]}
  :tooling []}

 {:id :cap/action/plan-item-invoice-mark-paid
  :title "Plan Item Invoice Mark Paid"
  :version "v1"
  :purpose "Ensure an invoice exists for a plan item, set status to paid, and issue (or reuse) the immutable invoice PDF."
  :inputs {:agreement/id :uuid
           :plan.item/id :uuid}
  :outputs {:invoice :map
            :invoice-pdf :map}
  :side-effects ["Create missing invoice for plan item"
                 "Mark invoice paid"
                 "Auto-issue (or reuse) immutable invoice document"]
  :adapter {:integration :local/db}
  :audit {:records [:invoice/change]
          :fields [:agreement/id :plan.item/id]}
  :idempotency {:strategy :dedupe-on
                :keys [:agreement/id :plan.item/id]}
  :contracts {:fixtures []}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/payment-plan-item :cap/schema/agreement :cap/schema/invoice :cap/schema/document]
            :views [:cap/view/clients]}
  :tooling []}

 {:id :cap/action/agreement-generate-due-invoices
  :title "Agreement Generate Due Invoices"
  :version "v1"
  :purpose "Generate invoices for plan items that are due within a window."
  :inputs {:agreement/id :uuid?
           :window/days :long?}
  :outputs {:invoices :vector}
  :side-effects ["Create missing invoices"
                 "Auto-issue (or reuse) immutable invoice documents for generated invoices"]
  :adapter {:integration :local/db}
  :audit {:records [:invoice/change]
          :fields [:agreement/id :window/days]}
  :idempotency {:strategy :none}
  :contracts {:fixtures []}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/agreement :cap/schema/payment-plan-item :cap/schema/invoice :cap/schema/document]
            :views [:cap/view/clients]}
  :tooling []}

 {:id :cap/action/invoice-create
  :title "Invoice Create"
  :version "v1"
  :purpose "Create an invoice for a client."
  :inputs {:client/id :uuid
           :agreement/id :uuid?
           :plan.item/id :uuid?
           :invoice/number :string
           :invoice/title :string?
           :invoice/description :string?
           :invoice/issued-at :instant?
           :invoice/due-at :instant?
           :invoice/currency :string?
           :invoice/total-amount :number
           :invoice/status :keyword?}
  :outputs {:invoice :map}
  :side-effects ["Persist invoice"]
  :adapter {:integration :local/db}
  :audit {:records [:invoice/change]
          :fields [:client/id :invoice/number]}
  :idempotency {:strategy :none}
  :contracts {:fixtures []}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/invoice :cap/schema/client :cap/schema/agreement :cap/schema/payment-plan-item]
            :views [:cap/view/clients]}
  :tooling []}

 {:id :cap/action/invoice-update
  :title "Invoice Update"
  :version "v1"
  :purpose "Update an invoice (status/title/description/due date)."
  :inputs {:invoice/id :uuid
           :invoice/title :string?
           :invoice/description :string?
           :invoice/due-at :instant?
           :invoice/status :keyword?}
  :outputs {:invoice :map}
  :side-effects ["Persist invoice updates"]
  :adapter {:integration :local/db}
  :audit {:records [:invoice/change]
          :fields [:invoice/id]}
  :idempotency {:strategy :dedupe-on
                :keys [:invoice/id]}
  :contracts {:fixtures []}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/invoice]
            :views [:cap/view/clients]}
  :tooling []}

 {:id :cap/action/invoice-list
  :title "Invoice List"
  :version "v1"
  :purpose "List invoices for a client."
  :inputs {:client/id :uuid}
  :outputs {:invoices :vector}
  :side-effects []
  :adapter {:integration :local/db}
  :audit {:records [:invoice/read]
          :fields [:client/id]}
  :idempotency {:strategy :pure}
  :contracts {:fixtures []}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/invoice :cap/schema/client]
            :views [:cap/view/clients]}
  :tooling []}

 {:id :cap/action/payment-create
  :title "Payment Create"
  :version "v1"
  :purpose "Create a payment record (optionally against an invoice)."
  :inputs {:client/id :uuid
           :agreement/id :uuid?
           :plan.item/id :uuid?
           :invoice/id :uuid?
           :payment/paid-at :instant?
           :payment/currency :string?
           :payment/amount :number
           :payment/method :keyword?
           :payment/reference :string?
           :payment/note :string?}
  :outputs {:payment :map}
  :side-effects ["Persist payment"
                 "Issue (or reuse) an immutable receipt document for the payment"]
  :adapter {:integration :local/db}
  :audit {:records [:payment/change]
          :fields [:client/id :invoice/id :payment/amount]}
  :idempotency {:strategy :none}
  :contracts {:fixtures []}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/payment :cap/schema/invoice :cap/schema/agreement :cap/schema/payment-plan-item :cap/schema/client]
            :views [:cap/view/clients]}
  :tooling []}

 {:id :cap/action/payment-list
  :title "Payment List"
  :version "v1"
  :purpose "List payments for a client."
  :inputs {:client/id :uuid}
  :outputs {:payments :vector}
  :side-effects []
  :adapter {:integration :local/db}
  :audit {:records [:payment/read]
          :fields [:client/id]}
  :idempotency {:strategy :pure}
  :contracts {:fixtures []}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/payment :cap/schema/client]
            :views [:cap/view/clients]}
  :tooling []}

 {:id :cap/action/proposal-generate
  :title "Proposal Generate"
  :version "v1"
  :purpose "Generate a proposal PDF from stored doc-pack data."
  :inputs {:client/id :uuid
           :file/slug :string?}
  :outputs {:file/id :uuid
            :file/url :string
            :file/slug :string?}
  :side-effects ["Render PDF via Playwright" "Store PDF in file library"]
  :adapter {:integration :cap/integration/local-file-store
            :renderer :cap/tooling/documents-pdf}
  :audit {:records [:document/generate]
          :fields [:client/id]}
  :idempotency {:strategy :dedupe-on
                :keys [:client/id]}
  :contracts {:fixtures []}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/doc-pack :cap/schema/document :cap/schema/file]
            :views [:cap/view/file-library]}
  :tooling [:cap/tooling/documents-pdf]}

 {:id :cap/action/invoice-pdf-generate
  :title "Invoice PDF Generate"
  :version "v1"
  :purpose "Generate an invoice PDF from stored invoice data."
  :inputs {:client/id :uuid
           :invoice/id :uuid
           :file/slug :string?}
  :outputs {:file/id :uuid
            :file/url :string
            :file/slug :string?}
  :side-effects ["Render PDF via Playwright" "Store PDF in file library"]
  :adapter {:integration :cap/integration/local-file-store
            :renderer :cap/tooling/documents-pdf}
  :audit {:records [:document/generate]
          :fields [:client/id :invoice/id]}
  :idempotency {:strategy :dedupe-on
                :keys [:invoice/id]}
  :contracts {:fixtures []}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/invoice :cap/schema/document :cap/schema/file]
            :views [:cap/view/file-library]}
  :tooling [:cap/tooling/documents-pdf]}

 {:id :cap/action/receipt-generate
  :title "Receipt Generate"
  :version "v1"
  :purpose "Generate a receipt PDF for a payment."
  :inputs {:client/id :uuid
           :payment/id :uuid
           :file/slug :string?}
  :outputs {:file/id :uuid
            :file/url :string
            :file/slug :string?}
  :side-effects ["Render PDF via Playwright" "Store PDF in file library"]
  :adapter {:integration :cap/integration/local-file-store
            :renderer :cap/tooling/documents-pdf}
  :audit {:records [:document/generate]
          :fields [:client/id :payment/id]}
  :idempotency {:strategy :dedupe-on
                :keys [:payment/id]}
  :contracts {:fixtures []}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/payment :cap/schema/document :cap/schema/file]
            :views [:cap/view/file-library]}
  :tooling [:cap/tooling/documents-pdf]}

 {:id :cap/action/status-report-generate
  :title "Status Report Generate"
  :version "v1"
  :purpose "Generate a status report PDF for a client (paid vs done vs remaining)."
  :inputs {:client/id :uuid
           :file/slug :string?}
  :outputs {:file/id :uuid
            :file/url :string
            :file/slug :string?}
  :side-effects ["Render PDF via Playwright" "Store PDF in file library"]
  :adapter {:integration :cap/integration/local-file-store
            :renderer :cap/tooling/documents-pdf}
  :audit {:records [:document/generate]
          :fields [:client/id]}
  :idempotency {:strategy :dedupe-on
                :keys [:client/id]}
  :contracts {:fixtures []}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/doc-pack :cap/schema/invoice :cap/schema/payment :cap/schema/document :cap/schema/file]
            :views [:cap/view/file-library]}
  :tooling [:cap/tooling/documents-pdf]}

 {:id :cap/action/document-issue
  :title "Document Issue"
  :version "v1"
  :purpose "Issue an immutable document (or return an existing issued copy when unchanged)."
  :inputs {:document/type :keyword
           :client/id :uuid
           :agreement/id :uuid?
           :invoice/id :uuid?
           :payment/id :uuid?
           :document/force? :boolean?
           :file/slug :string?}
  :outputs {:document :map
            :file :map}
  :side-effects ["Render PDF" "Store PDF file" "Persist issued document record"]
  :adapter {:integration :cap/integration/local-file-store
            :renderer :cap/tooling/documents-pdf}
  :audit {:records [:document/issue]
          :fields [:document/type :client/id :agreement/id :invoice/id :payment/id]}
  :idempotency {:strategy :dedupe-on
                :keys [:document/type :client/id :agreement/id :invoice/id :payment/id :document/force?]}
  :contracts {:fixtures []}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/document :cap/schema/file :cap/schema/client :cap/schema/agreement :cap/schema/invoice :cap/schema/payment]
            :views [:cap/view/file-library]}
  :tooling [:cap/tooling/documents-pdf]}

 {:id :cap/action/document-latest
  :title "Document Latest"
  :version "v1"
  :purpose "Fetch the latest issued document for a given subject and type."
  :inputs {:document/type :keyword
           :client/id :uuid
           :agreement/id :uuid?
           :invoice/id :uuid?
           :payment/id :uuid?}
  :outputs {:document :map
            :file :map}
  :side-effects []
  :adapter {:integration :local/db}
  :audit {:records [:document/read]
          :fields [:document/type :client/id :agreement/id :invoice/id :payment/id]}
  :idempotency {:strategy :pure}
  :contracts {:fixtures []}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/document :cap/schema/file :cap/schema/client :cap/schema/agreement :cap/schema/invoice :cap/schema/payment]
            :views [:cap/view/file-library]}
  :tooling []}

 {:id :cap/action/document-verify
  :title "Document Verify"
  :version "v1"
  :purpose "Verify a document ref + verification code pair (server-side, deterministic)."
  :inputs {:document/ref :string
           :document/verification-code :string}
  :outputs {:document/valid? :boolean
            :document :map?}
  :side-effects []
  :adapter {:integration :local/db}
  :audit {:records [:document/verify]
          :fields [:document/ref]}
  :idempotency {:strategy :pure}
  :contracts {:fixtures []}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/document]
            :views []}
  :tooling []}

 {:id :cap/action/analytics-revenue-by-month
  :title "Analytics Revenue By Month"
  :version "v1"
  :purpose "Sum payments by calendar month."
  :inputs {:client/id :uuid?
           :from :instant?
           :to :instant?}
  :outputs {:series :vector}
  :side-effects []
  :adapter {:integration :local/db}
  :audit {:records [:analytics/read]
          :fields [:from :to :client/id]}
  :idempotency {:strategy :pure}
  :contracts {:fixtures []}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/payment :cap/schema/client]
            :views []}
  :tooling []}

 {:id :cap/action/analytics-outstanding-invoices
  :title "Analytics Outstanding Invoices"
  :version "v1"
  :purpose "Compute outstanding balance per invoice and totals."
  :inputs {:client/id :uuid?
           :as-of :instant?}
  :outputs {:invoices :vector
            :totals :map}
  :side-effects []
  :adapter {:integration :local/db}
  :audit {:records [:analytics/read]
          :fields [:client/id :as-of]}
  :idempotency {:strategy :pure}
  :contracts {:fixtures []}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/invoice :cap/schema/payment :cap/schema/client]
            :views []}
  :tooling []}

 {:id :cap/action/analytics-funnel
  :title "Analytics Document Funnel"
  :version "v1"
  :purpose "Count issued documents by type (proposal → invoice → receipt) for a period."
  :inputs {:from :instant?
           :to :instant?
           :client/id :uuid?}
  :outputs {:counts :map}
  :side-effects []
  :adapter {:integration :local/db}
  :audit {:records [:analytics/read]
          :fields [:from :to :client/id]}
  :idempotency {:strategy :pure}
  :contracts {:fixtures []}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/document :cap/schema/client]
            :views []}
  :tooling []}

 {:id :cap/action/workspace-promote
  :title "Workspace Promote"
  :version "v1"
  :inputs {:workspace/id :string?
           :workspace/target :string?}
  :outputs {:promotion :map}
  :side-effects ["Move workspace-scoped entities to target workspace"]
  :adapter {:integration :local/db}
  :audit {:records [:workspace/promote]
          :fields [:workspace/id :workspace/target]}
  :idempotency {:strategy :none}
  :contracts {:fixtures [:fixtures/tasks :fixtures/tags]}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/task :cap/schema/tag :cap/schema/file :cap/schema/note]}
  :tooling []}

 {:id :cap/action/github-pulls
  :title "GitHub Pull Requests"
  :version "v1"
  :inputs {:pr/state :string?}
  :outputs {:pulls :list}
  :side-effects ["Fetch PR data from GitHub API"]
  :adapter {:integration :cap/integration/github}
  :audit {:records [:github/pulls]
          :fields [:pr/state]}
  :idempotency {:strategy :pure}
  :contracts {:fixtures []}
  :compatibility {:backward true
                  :forward true
                  :flags []}
 :related {:schema []
           :views [:cap/view/pr-overview]}
 :tooling []}

 {:id :cap/action/github-close-pr
  :title "GitHub Close Pull Request"
  :version "v1"
  :inputs {:pr/number :long}
  :outputs {:pr :map}
  :side-effects ["Close a PR via the GitHub API"]
  :adapter {:integration :cap/integration/github}
  :audit {:records [:github/pull-close]
          :fields [:pr/number]}
  :idempotency {:strategy :none}
  :contracts {:fixtures []}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema []
            :views [:cap/view/pr-overview]}
  :tooling []}

 {:id :cap/action/system-services
  :title "List System Services"
  :version "v1"
  :inputs {}
  :outputs {:services :vector}
  :side-effects []
  :adapter {:integration :local/system}
  :audit {:records [:system/read]
          :fields []}
  :idempotency {:strategy :pure}
  :contracts {:fixtures []}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema []
            :views [:cap/view/system-services]}
  :tooling []}

 {:id :cap/action/system-service-restart
  :title "Restart System Service"
  :version "v1"
  :inputs {:service/id :string}
  :outputs {:status :string}
  :side-effects ["Restart service"]
  :adapter {:integration :local/system}
  :audit {:records [:system/restart]
          :fields [:service/id]}
  :idempotency {:strategy :dedupe-on
                :keys [:service/id]}
  :contracts {:fixtures []}
  :compatibility {:backward true
                  :forward true
                  :flags []}
 :related {:schema []
           :views [:cap/view/system-services]}
 :tooling []}

 {:id :cap/action/agent-control-admins
  :title "Agent Control: Admins + Users"
  :version "v1"
  :inputs {}
  :outputs {:admins :vector
            :users :vector}
  :side-effects []
  :adapter {:integration :local/app}
  :audit {:records [:agent-control/read]
          :fields []}
  :idempotency {:strategy :pure}
  :contracts {:fixtures [:fixtures/users]}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/user]
            :views [:cap/view/agent-control]}
  :tooling []}

 {:id :cap/action/agent-control-run-list
  :title "Agent Control: List Runs"
  :version "v1"
  :inputs {}
  :outputs {:runs :vector}
  :side-effects []
  :adapter {:integration :local/app}
  :audit {:records [:agent-control/read]
          :fields []}
  :idempotency {:strategy :pure}
  :contracts {:fixtures [:fixtures/users]}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema []
            :views [:cap/view/agent-control]}
  :tooling []}

 {:id :cap/action/agent-control-run-create
  :title "Agent Control: Create Run"
 :version "v1"
 :inputs {:id :string?
          :title :string?
          :mode :string?
          :message :string}
 :outputs {:id :string
           :status :string}
 :side-effects ["Create run record on disk"]
 :adapter {:integration :local/app}
  :audit {:records [:agent-control/change]
          :fields [:id]}
  :idempotency {:strategy :dedupe-on
                :keys [:id]}
  :contracts {:fixtures [:fixtures/users]}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema []
            :views [:cap/view/agent-control]}
  :tooling []}

 {:id :cap/action/agent-control-run-detail
  :title "Agent Control: Run Detail"
  :version "v1"
  :inputs {:id :string}
  :outputs {:run :map}
  :side-effects []
  :adapter {:integration :local/app}
  :audit {:records [:agent-control/read]
          :fields [:id]}
  :idempotency {:strategy :pure}
  :contracts {:fixtures [:fixtures/users]}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema []
            :views [:cap/view/agent-control]}
  :tooling []}

 {:id :cap/action/agent-control-run-revise
  :title "Agent Control: Revise Run"
  :version "v1"
  :inputs {:id :string
           :message :string}
  :outputs {:run :map}
  :side-effects ["Append revision to run record"]
  :adapter {:integration :local/app}
  :audit {:records [:agent-control/change]
          :fields [:id]}
  :idempotency {:strategy :append-only}
  :contracts {:fixtures [:fixtures/users]}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema []
            :views [:cap/view/agent-control]}
  :tooling []}

 {:id :cap/action/agent-control-preview-start
  :title "Agent Control: Start/Update Preview"
  :version "v1"
  :inputs {:id :string
           :mode :string?}
  :outputs {:job :string
            :run_id :string}
  :side-effects ["Starts preview process and runs verification"]
  :adapter {:integration :local/scripts}
  :audit {:records [:agent-control/execute]
          :fields [:id]}
  :idempotency {:strategy :dedupe-on
                :keys [:id]}
  :contracts {:fixtures [:fixtures/users]}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema []
            :views [:cap/view/agent-control]}
  :tooling []}

 {:id :cap/action/agent-control-accept
  :title "Agent Control: Accept"
  :version "v1"
  :inputs {:id :string}
  :outputs {:job :string
            :run_id :string}
  :side-effects ["Marks the run as accepted (no promotion)"]
  :adapter {:integration :local/scripts}
  :audit {:records [:agent-control/execute]
          :fields [:id]}
  :idempotency {:strategy :dedupe-on
                :keys [:id]}
  :contracts {:fixtures [:fixtures/users]}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema []
            :views [:cap/view/agent-control]}
  :tooling []}

 {:id :cap/action/agent-control-trash
  :title "Agent Control: Trash Run"
  :version "v1"
  :inputs {:id :string}
  :outputs {:job :string
            :run_id :string}
  :side-effects ["Stops preview and marks run as trashed"]
  :adapter {:integration :local/scripts}
  :audit {:records [:agent-control/execute]
          :fields [:id]}
  :idempotency {:strategy :dedupe-on
                :keys [:id]}
  :contracts {:fixtures [:fixtures/users]}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema []
            :views [:cap/view/agent-control]}
  :tooling []}

 {:id :cap/action/agent-control-job-log
  :title "Agent Control: Job Log"
  :version "v1"
  :inputs {:id :string
           :job :string}
  :outputs {:log :string}
  :side-effects []
  :adapter {:integration :local/app}
  :audit {:records [:agent-control/read]
          :fields [:id :job]}
  :idempotency {:strategy :pure}
  :contracts {:fixtures [:fixtures/users]}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema []
            :views [:cap/view/agent-control]}
  :tooling []}

 {:id :cap/action/service-list
  :title "List Services"
  :version "v1"
  :inputs {}
  :outputs {:services :vector}
  :side-effects []
  :adapter {:integration :local/registries}
  :audit {:records [:services/read]
          :fields []}
  :idempotency {:strategy :pure}
  :contracts {:fixtures [:fixtures/users]}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/service-case]
            :views []}
  :tooling []}

 {:id :cap/action/service-case-create
  :title "Create Service Case"
  :version "v1"
  :inputs {:client/id :uuid
           :service/id :keyword
           :service.case/title :string?
           :service.case/assignee :uuid?}
  :outputs {:service.case/id :uuid
            :service.case/steps :vector}
  :side-effects ["Persist service case and initial steps" "May create internal tasks for next steps"]
  :adapter {:integration :local/db}
  :audit {:records [:service-case/change]
          :fields [:client/id :service/id :service.case/id]}
  :idempotency {:strategy :dedupe-on
                :keys [:client/id :service/id :service.case/title]}
  :contracts {:fixtures [:fixtures/users :fixtures/clients]}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/service-case :cap/schema/service-case-step :cap/schema/task]
            :views []}
  :tooling []}

 {:id :cap/action/service-case-list
  :title "List Service Cases"
  :version "v1"
  :inputs {:client/id :uuid}
  :outputs {:service.cases :vector}
  :side-effects []
  :adapter {:integration :local/db}
  :audit {:records [:service-case/read]
          :fields [:client/id]}
  :idempotency {:strategy :pure}
  :contracts {:fixtures [:fixtures/users :fixtures/clients]}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/service-case :cap/schema/service-case-step]
            :views []}
  :tooling []}

 {:id :cap/action/service-case-read
  :title "Read Service Case"
  :version "v1"
  :inputs {:service.case/id :uuid}
  :outputs {:service.case/id :uuid
            :service.case/steps :vector}
  :side-effects []
  :adapter {:integration :local/db}
  :audit {:records [:service-case/read]
          :fields [:service.case/id]}
  :idempotency {:strategy :pure}
  :contracts {:fixtures [:fixtures/users :fixtures/clients]}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/service-case :cap/schema/service-case-step]
            :views []}
  :tooling []}

 {:id :cap/action/service-case-step-set-status
  :title "Set Service Case Step Status"
  :version "v1"
  :inputs {:service.case/id :uuid
           :service.case.step/step :keyword
           :service.case.step/status :keyword
           :service.case.step/reason :string?}
  :outputs {:service.case/id :uuid
            :service.case/steps :vector}
  :side-effects ["Persist step status change" "May update case lifecycle and public progress" "May create internal tasks for next steps"]
  :adapter {:integration :local/db}
  :audit {:records [:service-case/change]
          :fields [:service.case/id :service.case.step/step :service.case.step/status]}
  :idempotency {:strategy :dedupe-on
                :keys [:service.case/id :service.case.step/step :service.case.step/status :service.case.step/reason]}
  :contracts {:fixtures [:fixtures/users :fixtures/clients]}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/service-case :cap/schema/service-case-step :cap/schema/task]
            :views []}
  :tooling []}

 {:id :cap/action/client-portal-link
  :title "Get Client Portal Link"
  :version "v1"
  :inputs {:client/id :uuid}
  :outputs {:portal/url :string
            :client/ref :string}
  :side-effects ["May issue/rotate a client portal token"]
  :adapter {:integration :local/db}
  :audit {:records [:portal/read]
          :fields [:client/id]}
  :idempotency {:strategy :dedupe-on
                :keys [:client/id]}
  :contracts {:fixtures [:fixtures/users :fixtures/clients]}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/client :cap/schema/service-case]
            :views []}
  :tooling []}

 {:id :cap/action/report-card-submit
  :title "Report Card Submit"
  :version "v1"
  :inputs {:report.card/type :keyword
           :task/id :uuid
           :client/id :uuid
           :report.card/fields :string}
  :outputs {:report.card/id :uuid
            :task/id :uuid
            :next-task/id :uuid?}
  :side-effects ["Persist report card" "Mark source task done" "Create follow-up task (stub)"]
  :adapter {:integration :local/db}
  :audit {:records [:report-card/change]
          :fields [:report.card/type :task/id :client/id]}
  :idempotency {:strategy :dedupe-on
                :keys [:report.card/type :task/id]}
  :contracts {:fixtures [:fixtures/users :fixtures/clients :fixtures/tasks]}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/report-card :cap/schema/task :cap/schema/client]
            :views []}
  :tooling []}

 {:id :cap/action/telegram-notify
  :title "Telegram Notify"
  :version "v1"
  :inputs {:user/id :uuid
           :telegram/text :string
           :telegram/message-key :string?}
  :outputs {:status :keyword}
  :side-effects ["Enqueue telegram outbox message when user has a chat id"]
  :adapter {:integration :integration/telegram}
  :audit {:records [:telegram/notify]
          :fields [:user/id]}
  :idempotency {:strategy :dedupe-on
                :keys [:telegram/message-key]}
  :contracts {:fixtures [:fixtures/users]}
  :compatibility {:backward true
                  :forward true
                  :flags []}
  :related {:schema [:cap/schema/user :cap/schema/outbox]
            :views []}
  :tooling []}
]
